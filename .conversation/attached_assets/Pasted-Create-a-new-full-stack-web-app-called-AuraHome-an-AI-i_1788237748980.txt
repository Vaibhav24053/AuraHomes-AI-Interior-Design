Create a new full-stack web app called AuraHome — an AI interior design platform for the Indian market.

TECH STACK:
- Frontend: React + Vite + TypeScript, Tailwind CSS v4 (CSS-first config via @theme, no tailwind.config.js), shadcn/ui components, react-router-dom, framer-motion for animation, lucide-react for icons
- Backend: Express + TypeScript, using OpenAI's SDK (I will provide API keys as environment secrets: OPENAI_API_KEY)
- No database needed yet — that comes later

DESIGN SYSTEM — set this up first, in src/index.css:
- Google Fonts: 'DM Serif Display' for headlines, 'DM Sans' for body text
- Base colors: warm cream background (#FDFAF6 to #F5F0E8 gradient range), warm near-black text/dark sections (#1C1917), gold accent (#C8A97E, hover #B8976C)
- New accent colors for feature categories (add as Tailwind @theme tokens): terracotta (#B85C38) for Vastu/tradition features, deep teal (#1F5C57) for AR/tech features, brass (#8B6B2E) for sourcing/marketplace features, sage (#6B7A5E) for sustainability features. These are used sparingly as category signal colors, not a full repaint.
- Motion style: soft, warm, editorial — gentle fade-ins on scroll (intersection observer reveal pattern), floating blob background shapes with subtle animation, a marquee ticker, tilt-on-hover cards, gold gradient CTA buttons with glow on hover. Favor smooth cross-fades over hard cuts.

BUILD THE LANDING PAGE with these sections, in order:
1. Hero — full viewport height, gradient cream background with 2-3 animated floating blur blobs, small pill badge "AI-Powered Interior Design", large serif headline "Your Dream Home, Designed by AI" with animated gradient text effect, subheading copy, two CTA buttons ("Transform My Room" primary gold, "Take the Style Quiz" outline), social proof row with avatar stack + "2,400+ Indian homes transformed this month", animated scroll indicator at bottom
2. A dark marquee ticker scrolling style names: Japandi, Bohemian, Kerala Traditional, Punjabi Maximalist, Gen-Z Minimal, Vastu-Modern, Rajasthani, Bengali, Industrial Loft, Coastal
3. Before/After section — a draggable before/after image comparison slider component (build this as a reusable component), placeholder images for now
4. Stats bar — 4 animated count-up stats (use an IntersectionObserver-triggered counter hook): "12,400+ Rooms Transformed", "6 Regional Styles", "98% Satisfaction", "45s Avg Generation Time"
5. "Everything You Need" toolkit section — ASYMMETRIC grid (not uniform 2x2): one large featured card for "Design DNA Quiz" (our headline feature) plus 3 smaller cards for "Vastu-Corrected Redesign", "AR Room View", "Real-Price Sourcing" — each links to a route (/quiz, /transform, /transform, /budget)
6. Dark "Built for Indian Homes" section with 3 feature cards: Vastu-Modern Fusion (terracotta accent), Rent-Friendly No-Drill Design (deep-teal accent), Real Prices in INR from Local + Artisan Sources (brass accent)
7. Footer with AuraHome logo, tagline, and Product/Community/Connect link columns

Also build:
- A persistent Navbar with logo, links to Home/Transform/Quiz/Budget/Community, and a "Get Started" button
- Set up React Router with placeholder pages for /transform, /quiz, /budget, /community (just a heading for now, we'll build these next)

Keep everything in this first pass to UI/layout/animation only — no backend calls, no real logic yet. Use Unsplash placeholder images where needed.